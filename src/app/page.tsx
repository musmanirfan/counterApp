

import Counter from "./components/counter";
import AnimatedComponent from "./components/AnimatedComponent";

export default function Home({}) {

  return (
    <>
      <Counter count={5} message={`Hello`} />
    
    </>
  );
}
